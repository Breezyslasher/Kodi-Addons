<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="plugin.video.tubitv" name="TubiTV" version="2026.8.11" provider-name="Lunatixz, Breezyslasher">
	<requires>
		<import addon="xbmc.python" version="3.0.0"/>
		<import addon="script.module.t1mlib" version="4.0.9"/>
		<import addon="script.module.requests" version="2.22.0"/>
		<import addon="inputstream.adaptive" version="2.6.0"/>
	</requires>
	<extension point="xbmc.python.pluginsource" library="default.py">
		<provides>video</provides>
	</extension>
	<extension point="xbmc.service" library="pseudotv_recommended.py"/>
	<extension point="xbmc.addon.metadata">
		<summary lang="en_GB">Tubi TV</summary>
		<description lang="en_GB">Tubi TV has the largest library of FREE videos of hit and cult TV shows and films.

* Browse the Tubi categories, series and films
* Live TV, with IPTV Manager integration for the Kodi TV guide
* Search the Tubi catalogue
* Add films and shows to the Kodi library as .strm files
* Optional Tubi account sign-in
* PseudoTV Live support</description>
		<disclaimer lang="en_GB">Feel free to use this script. For information visit the wiki.</disclaimer>
		<platform>all</platform>
		<language>en</language>
		<license>GPL-2.0-or-later</license>
		<forum>http://forum.kodi.tv/showthread.php?tid=</forum>
		<website>http://www.tubitv.com/</website>
		<source>https://github.com/Breezyslasher/Kodi-Addons</source>
		<news>
2026.8.11 - Modern list items
- FIXED: Every list item logged two deprecation warnings, because t1mlib
  still builds them with ListItem.setInfo() and addStreamInfo(). The addon
  now fills the video info tag through its own setters on Kodi 20 and
  later, falling back to the old calls on Kodi 19 which has nothing else.

2026.8.10.8 - Guide text
- FIXED: Some guide titles and descriptions arrived HTML-escaped and were
  passed on as-is, so Kodi displayed "Amos &amp;amp; Andrew" and
  "caf&amp;eacute;" literally. Entities are now resolved to the characters
  they stand for.

2026.8.10.7 - Faster live guide
- CHANGED: The guide's batches are now fetched a few at a time instead of
  one after another, cutting a refresh from tens of seconds to a few. The
  tools that ask for it are waiting on a socket while it is built.
- NEW: The addon logs how many channels and programmes it handed over and
  how long it took, so an empty guide can be told apart from one that was
  never asked for.

2026.8.10.6 - Live guide fix
- FIXED: The channels reached the Kodi TV guide but the programme
  schedule never did. IPTV Manager allows ten seconds for the addon to
  call back, and the addon was building the whole guide - one request per
  batch of channels - before opening the connection. It now connects
  first and builds afterwards, which is what IPTV Manager waits for.
- FIXED: One failed batch no longer discards the entire guide.

2026.8.10.5 - Live TV
- NEW: Tubi's 177 linear channels, with a Live TV folder in the addon and
  IPTV Manager integration so they appear in the Kodi TV guide with a
  full programme schedule.

2026.8.10.4 - Current DRM properties
- CHANGED: On inputstream.adaptive 21 and later the DRM configuration is
  passed through drm_legacy, which clears the deprecation warning about
  the license_type and license_key properties. Older builds, which only
  understand the old properties, keep using them.

2026.8.10.3 - DRM playback fix
- FIXED: Encrypted titles failed to decode with "Failed to initialize a
  DRM session". Tubi grades the same film twice - its 720p renditions
  demand HDCP and Widevine security level 2, its 576p ones demand
  neither - and the addon was picking the protected one, which a software
  CDM cannot satisfy. It now prefers the rendition that plays without
  HDCP.
- NEW: "Allow HDCP protected streams" for devices with hardware DRM that
  can play the higher quality renditions.

2026.8.10.2 - Search fix
- FIXED: Opening Search crashed the addon. The menu entry carried an
  empty url, Kodi trimmed the '=' off it, and t1mlib could not parse the
  query back. The entry now carries a value, and default.py repairs a
  trimmed query before handing it over.

2026.8.10.1 - Moved to Tubi's current API
- FIXED: Browsing, search and playback have moved off the retired /oz
  endpoints and onto the API the Tubi site itself now uses. Nothing in
  the addon reached a live endpoint before this.
- NEW: Categories page properly, series list their seasons, and search
  returns the full result set.
- NEW: Widevine playback through inputstream.adaptive for the titles Tubi
  encrypts. Titles served in the clear still play without it.

2026.8.10 - Sign-in repaired
- FIXED: Signing in no longer uses the retired /oz/auth/login endpoint. The
  addon now performs the same handshake as the Tubi web client: a signed
  anonymous device token, then the account login, then the web session
  handover that yields the cookie the content calls need.
- FIXED: The addon no longer dies on startup when the sign-in fails. Tubi is
  free to browse, so it carries on anonymously and reports the problem.
- FIXED: Tokens are cached in the addon profile instead of being fetched on
  every single browse and playback.
- FIXED: Translations are read again (moved to the Kodi 19+ language layout).
- NEW: "Clear saved session" in settings for a clean sign-in.
		</news>
		<assets>
			<icon>resources/images/icon.png</icon>
			<fanart>resources/images/fanart.jpg</fanart>
		</assets>
	</extension>
</addon>
